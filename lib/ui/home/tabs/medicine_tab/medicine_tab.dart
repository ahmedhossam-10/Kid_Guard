import 'package:flutter/material.dart';

class MedicineTab extends StatelessWidget {
  const MedicineTab({super.key});
  static const String routeName = 'medicineTab';

  @override
  Widget build(BuildContext context) {
     return Container(
        color: Colors.cyan,
     );
  }
}
