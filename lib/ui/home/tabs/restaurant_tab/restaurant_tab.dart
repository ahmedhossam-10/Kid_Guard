import 'package:flutter/material.dart';

class RestaurantTab extends StatelessWidget {
  const RestaurantTab({super.key});
  static const String routeName = 'restaurantTab';

  @override
  Widget build(BuildContext context) {
    return Container(
      color: Colors.black,
    );
  }
}
